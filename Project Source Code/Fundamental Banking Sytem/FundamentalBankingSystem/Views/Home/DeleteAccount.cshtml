@model FundamentalBankingSystem.Models.sp_ViewAccountsByID_Result

@{
    ViewBag.Title = "DeleteAccount";
    Layout = "~/Views/Shared/_ManagerLayout.cshtml";
}

<div class="container2" style="margin-left:150px">

    <p style="font-size:x-large">Delete Account</p>
    <p style="font-size:large">Are you sure you want to delete this account?</p>
    <dl class="dl-horizontal">

        <dt style="text-align:left">
            @Html.DisplayName("Customer ID")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CustomerId)
        </dd>

        <dt style="text-align:left">
            @Html.DisplayName("Account Number")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AccountNumber)
        </dd>

        <dt style="text-align:left">
            @Html.DisplayName("Account Type")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AccountType)
        </dd>

        <dt style="text-align:left">
            @Html.DisplayNameFor(model => model.Balance)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Balance)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        if (Session["roleid"] != null)
        {
            if (Session["roleid"].ToString() == "1")
            {

            }
            else
            {
                Response.Redirect("Logout");
            }
        }
        else
        {
            Response.Redirect("Logout");
        }

        @Html.HiddenFor(model => model.AccountNumber)
        @Html.HiddenFor(model => model.Balance)
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            @Html.ActionLink("Back to List", "ListAccount")
        </div>
    }
</div>
